# Outputs

- `figures/` — plots exported from analysis (e.g. feature importance, ROC curves).
- `submission/` — challenge / presentation artifacts (e.g. PDF briefs).

Regenerate figures with the `ml/` scripts if needed.
